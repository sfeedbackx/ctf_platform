export enum instanceState {
  RUNNING = 'RUNNING',
  STOPPED = 'STOPPED',
  PENDING = 'PENDING',
  FAILED = 'FAILED',
  TERMINATED = 'TERMINATED',
}

export enum serviceType {
  FRONTEND = 'FRONTEND',
  BACKEND = 'BACKEND',
  DB = 'DB',
}
